import { defineComponent, toRef, computed } from "vue";
import { u as useColors } from "../../../../composables/useColors-b582f249.mjs";
import { u as useTextColor } from "../../../../composables/useTextColor-48b60d5c.mjs";
const _sfc_main = defineComponent({
  name: "VaDropdownContent",
  props: {
    noPadding: { type: Boolean, default: false },
    background: { type: String, default: "background-secondary" },
    textColor: { type: String }
  },
  setup(props) {
    const { getColor } = useColors();
    const { textColorComputed } = useTextColor(toRef(props, "background"));
    const computedStyle = computed(() => ({
      background: getColor(props.background, void 0, true),
      color: textColorComputed.value,
      padding: props.noPadding ? 0 : void 0
    }));
    return { computedStyle };
  }
});
export {
  _sfc_main as _
};
//# sourceMappingURL=VaDropdownContent.vue_vue_type_script_lang-2b4f50d0.mjs.map
