const focusElement = (el) => {
  if (!el) {
    return;
  }
  el.focus();
  el.dispatchEvent(new FocusEvent("focus", { bubbles: true }));
};
const blurElement = (el) => {
  if (!el) {
    return;
  }
  el.blur();
  el.dispatchEvent(new Event("blur", { bubbles: true }));
};
export {
  blurElement as b,
  focusElement as f
};
//# sourceMappingURL=focus-685f6da0.mjs.map
